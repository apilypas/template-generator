<nlog autoReload="true">
    <targets>
        <target name="logconsole" type="Console" layout="${longdate}|${level:format=FirstCharacter}|${logger:shortName=true}|${message}" />
    </targets>

    <rules>
        <logger name="*" writeTo="logconsole" />
    </rules>
</nlog>
